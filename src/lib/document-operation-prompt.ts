export const DEFAULT_DOCUMENT_OPERATION_PROMPT = `When changing a Word, Excel, PDF, PowerPoint, or code artifact, create a reviewable draft instead of editing the source file. Split the requested result into the smallest user-visible operations that can be replayed safely. Give every operation one stable format-specific location, a concise before/after summary, an opaque persistent execution reference, and explicit dependencies. Put inseparable changes in the same atomic group. Order independent operations by document position from top to bottom, by worksheet and A1 range, by PDF page, by slide, or by code-file dependency and exact source offset. Treat recalculation, pagination, and layout movement as derived effects of the operation that caused them. Do not combine unrelated regions into one operation. For code, use workspace-issued artifact handles, exact before text and UTF-16 offsets; never use fuzzy matching. Submit only producer-issued opaque handles and never submit a local path or encoded file payload.`
