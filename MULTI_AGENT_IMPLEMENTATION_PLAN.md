# 多 Agent 可见协作系统实施说明

## 一、系统要解决的问题

系统的核心不是让多个模型同时输出文字，而是让一个用户指定的 Root Agent 负责理解目标、拆解任务、调度其他 Agent，并把所有思考、提问、回答和文件修改变成用户可以检查的实时事件。

文件修改必须发生在用户当前正在看的编辑器实例中。模型不能先在隐藏窗口里改完，再把结果一次性复制回来。这样才能让用户知道修改发生在哪里，也才能在错误操作继续扩散前按下停止。

## 二、具体思考逻辑

### 1. 任务进入

用户发送任务时，系统生成唯一的运行编号。运行编号会贯穿 Root Agent、子 Agent、文档操作、停止请求和前端时间线，因此任何一个事件都能追溯到同一次任务。

Root Agent 的角色由用户预先指定，例如规划者、实施者、审阅者或总负责人。角色决定它的系统提示和最终责任，不由系统临时猜测。

### 2. Root Agent 拆解

Root Agent 先读取当前文件上下文，再将目标拆成可验证的工作项。每个工作项至少包含目标、输入范围、预期结果、允许的文件操作和完成条件。

Root Agent 选择子 Agent 时参考它们的角色、厂商、模型、启用状态和用户配置。例如让审阅 Agent 只做检查，让实施 Agent 负责修改，让测试 Agent 只验证结果。

### 3. Agent 之间沟通

子 Agent 不会只收到一段孤立的提示，而会收到原始目标、自己的工作项、当前文档版本和此前 Agent 的结论。

当一个 Agent 把工作交给另一个 Agent 时，系统记录一次交接；当它向另一个 Agent 请求判断时，记录问题事件；对方完成回答后，记录回答事件。前端按时间顺序呈现这些事件，所以用户能看到“谁问谁、问了什么、谁如何回答”。

当前实现使用 Root Agent 选定的 Agent 队列保证顺序和可重复性，同时允许 Root 或子 Agent 主动向已配置的其他 Agent 提问或分派子任务。委派深度有限制，并沿用相同的运行编号和事件协议，避免循环调用。

### 4. 修改前的检查

每个修改在执行前都会生成操作编号，并记录目标文件、编辑器类型、基础版本和操作范围。操作先进入可见编辑器的串行队列，避免两个 Agent 同时写入造成内容交叉。

如果基础版本已经变化，操作会被标记为冲突并暂停，而不是覆盖用户刚刚输入的内容。用户编辑会递增文档版本，下一项 Agent 操作会校验本次运行期望的版本。

### 5. 修改中的反馈

操作执行过程中依次产生准备、光标移动、选区变化、应用成功或拒绝、版本变化等事件。事件同时提供给 Agent 时间线和当前编辑器，因此用户能在文档里看到实际光标，在侧栏里看到操作状态。

### 6. 停止逻辑

用户点击停止后，系统先中止模型运行，再向渲染进程发送同一个运行编号。渲染进程将尚未执行的操作标记为取消，队列不再取出新的修改。已经写入文档的内容保持可见，并归入编辑器的撤销历史。

停止不是删除已完成结果，而是阻止后续动作继续发生。这样用户可以保留正确部分，手工修正或重新发起剩余任务。

### 7. 最终收敛

所有子 Agent 完成后，Root Agent 再接收共享上下文进行最终审阅和总结。最终结果不仅包含文字回复，也包含已经应用的操作、被拒绝的操作、未完成的工作项和停止原因。

## 三、需要怎么改

### 1. Agent 编排层

- 为每次聊天和协作运行建立运行编号、取消控制器和事件发送器。
- 将 Root Agent、子 Agent、任务分派、交接、问题、回答、工具调用和最终汇总统一成可序列化事件。
- Root Agent 在协作弹窗中由用户明确选择，其他已启用 Agent 作为子 Agent 接收分派。
- 保留模型厂商和模型选择，不把不同厂商的模型伪装成同一个模型。
- 让错误、缺少模型、超时和用户停止都进入同一条状态链路。

### 2. 文档操作层

- 增加统一的文档操作描述：操作编号、运行编号、Agent、文件、编辑器、基础版本、操作类型和撤销分组。
- 增加串行操作队列，并支持按运行编号取消尚未执行的操作。
- 增加准备、光标、选区、成功、拒绝、撤销、冲突和版本变化事件。
- 文档桥接器只接受当前可见编辑器实例，不再把隐藏 Agent 窗口作为默认执行路径。

### 3. Word 文件修改

- SuperDoc 在完成初始化后注册为当前 Word 文档桥接器。
- 插入和追加操作直接使用当前 SuperDoc 编辑事务，因此内容立刻出现在用户正在看的页面。
- 文本替换遍历当前文档的实际文本节点并提交事务，不再通过“在末尾插入替换提示”模拟修改。
- Word 预览层监听操作事件，显示 Agent 名称、虚拟指针和操作状态；保存仍走当前文档的正常导出流程。
- 后续可以把指针从当前页面中心进一步提升为精确文本节点坐标，并增加按操作编号的撤销按钮。

### 4. 代码文件修改

- Monaco 初始化后把编辑器和文本模型注册到文档桥接器。
- 插入、替换和删除都使用 Monaco 的行号、列号和范围编辑，不通过整文件字符串替换。
- 修改前移动真实光标并滚动到目标行，修改后保留 Monaco 的选区、撤销栈、脏状态和保存流程。
- 搜索替换按文本偏移转换为行列范围，支持首个匹配和全部匹配。
- 编辑器叠加层显示 Agent 名称和虚拟光标，操作结束后自动收起，不遮挡代码内容。
- 后续可以增加变更块着色、逐 hunk 接受/拒绝和按 Agent 过滤。

### 5. Plain Text、Excel 和其他文件

- Plain Text 复用文本桥接和同一套队列、事件和取消协议。
- Excel 保留 Fortune Sheet 的实例边界，单元格写入作为独立操作进入同一版本链路。
- PDF 只读，不允许 Agent 直接写入；需要修改时必须转到可编辑文档类型。

### 6. OnlyOffice 可见兼容路径

- OnlyOffice 编辑器实例在当前渲染窗口注册可见命令桥接。
- 主进程通过 IPC 把命令发送给当前窗口，渲染器执行后回传操作结果。
- 如果当前 OnlyOffice 版本没有可用的插件命令 API，系统返回明确的“不支持可见命令”结果，不偷偷创建隐藏 Agent 编辑器。
- 当前默认路线仍是 SuperDoc 和 Monaco，因为它们可以直接访问当前文档事务和代码模型。

### 7. 前端界面

- Agent 侧栏保留单 Agent 微信式聊天，并在底部显示停止按钮。
- 协作配置弹窗显示启用的 Agent、厂商/模型和 Root Agent 选择。
- 协作时间线显示 Root Agent 分派、Agent 思考、Agent 问题、Agent 回答、交接、工具调用、文档操作、失败和停止。
- 时间线和编辑器都以运行编号与操作编号关联，用户可以从一条消息定位到一次实际修改。

## 四、用户能够得到什么

1. 用户可以指定“豆包负责指挥，GPT 负责实施，Claude 负责审阅”这样的协作结构，并明确指定 Root Agent。
2. 用户可以像看群聊一样看到 Agent 之间的思考、提问、回答和任务交接，而不是只看到最后一段总结。
3. Word 文档会在当前预览页面中实时变化，用户能看到虚拟指针和实际修改位置。
4. 代码文件会在当前 Monaco 编辑器中按行列实时修改，用户能看到光标跳转、范围变化和后续结果。
5. 用户可以在模型继续生成或下一项修改开始前停止运行；已经确认的修改不会被无故抹掉。
6. 每个修改都有可追踪的 Agent、运行编号、操作编号和版本信息，后续可以实现审批、冲突解决和逐块回滚。
7. 同一套交互可以扩展到更多模型厂商，而不需要为每个厂商单独设计一套前端沟通界面。

## 五、当前验证结果和可选增强

- 类型检查通过。
- 本地化键检查通过，9 个语言包保持一致。
- Agent 协作编排单测通过。
- Electron 协作弹窗验证通过，能显示两个 Agent、默认选中状态和 Root Agent 入口。
- 可见文档桥测试通过，覆盖 Word 插入与替换、Monaco 行列范围编辑、用户编辑冲突和运行取消。
- 代码编辑器完整验证通过 113/113 项，包括代码运行、标签切换、Markdown/TXT 表格编辑、滚动条和缩放。
- 当前已支持运行时在已配置 Agent 之间提问和委派，Word 文本定位和运行版本冲突也已接入。动态生成全新 Agent 配置和逐块审批不是本阶段必需项，可作为后续增强。
