{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimal capability set for local application windows. Privileged operations are exposed only through validated Rust commands.",
  "local": true,
  "windows": ["main", "document-*"],
  "permissions": ["core:default"]
}
