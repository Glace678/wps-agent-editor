# Generate a unique value, for example: openssl rand -base64 48
# Start Electron with this same OO_JWT_SECRET value.
OO_JWT_SECRET=

# Standalone development bridge only (npm run dev:bridge).
OO_SERVER_URL=http://127.0.0.1:8080
BRIDGE_PORT=3001
OO_ALLOWED_DOCUMENTS=[]
